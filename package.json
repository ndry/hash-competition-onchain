{
  "name": "@xplart/pow-onchain",
  "scripts": {
    "hardhat": "set NODE_OPTIONS=--openssl-legacy-provider && hardhat",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && hardhat test",
    "coverage": "set NODE_OPTIONS=--openssl-legacy-provider && hardhat coverage",
    "ts": "set NODE_OPTIONS=--openssl-legacy-provider && ts-node --transpile-only",
    "deploy-local": "hardhat run ./scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-waffle": "^2.0.2",
    "@typechain/ethers-v5": "^9.0.0",
    "@typechain/hardhat": "^4.0.0",
    "@types/chai": "^4.3.0",
    "@types/lodash": "^4.14.178",
    "@types/mocha": "^9.1.0",
    "chai": "^4.3.6",
    "dotenv": "^16.0.0",
    "hardhat": "^2.8.3",
    "lodash": "^4.17.21",
    "mocha": "^9.2.0",
    "solidity-coverage": "^0.7.18",
    "ts-node": "^10.5.0"
  }
}
